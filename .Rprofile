source("renv/activate.R")
options(renv.settings.vcs.ignore.local = "FALSE")
renv::settings$snapshot.type("all")
options(rstudio.help.showDataPreview = FALSE) # this is just a temporary fix to remove a weird error message:https://github.com/rstudio/rstudio/issues/13188
